# =============================================================
# Template: 2.5-nat-private-subnet.yaml
# Descripcion: Agrega una subnet privada con salida a Internet
#   mediante NAT Gateway en una subnet publica existente.
# Recursos creados:
#   - Private Subnet
#   - Elastic IP para NAT
#   - NAT Gateway (en subnet publica)
#   - Route Table privada
#   - Ruta por defecto (0.0.0.0/0) hacia NAT
#   - Asociacion subnet privada <-> route table privada
# Outputs:
#   - PrivateSubnetId
#   - NatGatewayId
#   - PrivateRouteTableId
# =============================================================
AWSTemplateFormatVersion: '2010-09-09'

Parameters:
  VpcId:
    Type: AWS::EC2::VPC::Id
    Description: VPC donde se creara la subnet privada

  PublicSubnetId:
    Type: AWS::EC2::Subnet::Id
    Description: Subnet publica existente donde se desplegara el NAT Gateway

  PrivateSubnetCidr:
    Type: String
    Default: 10.0.2.0/24
    Description: CIDR para la subnet privada

  AvailabilityZone:
    Type: AWS::EC2::AvailabilityZone::Name
    Default: us-east-1a
    Description: Zona de disponibilidad para la subnet privada

Resources:
  SubnetPrivate:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref VpcId
      CidrBlock: !Ref PrivateSubnetCidr
      AvailabilityZone: !Ref AvailabilityZone
      MapPublicIpOnLaunch: false

  NatElasticIp:
    Type: AWS::EC2::EIP
    Properties:
      Domain: vpc

  NatGatewayPrivate:
    Type: AWS::EC2::NatGateway
    Properties:
      AllocationId: !GetAtt NatElasticIp.AllocationId
      SubnetId: !Ref PublicSubnetId

  RouteTablePrivate:
    Type: AWS::EC2::RouteTable
    Properties:
      VpcId: !Ref VpcId

  RoutePrivateDefault:
    Type: AWS::EC2::Route
    Properties:
      RouteTableId: !Ref RouteTablePrivate
      DestinationCidrBlock: 0.0.0.0/0
      NatGatewayId: !Ref NatGatewayPrivate

  SubnetPrivateRouteTableAssociation:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      SubnetId: !Ref SubnetPrivate
      RouteTableId: !Ref RouteTablePrivate

Outputs:
  PrivateSubnetId:
    Description: ID de la subnet privada creada
    Value: !Ref SubnetPrivate

  NatGatewayId:
    Description: ID del NAT Gateway
    Value: !Ref NatGatewayPrivate

  PrivateRouteTableId:
    Description: ID de la route table privada
    Value: !Ref RouteTablePrivate
