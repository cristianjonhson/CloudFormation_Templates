# =============================================================
# Template: 3.5-ec2-ssm-no-ssh.yaml
# Descripcion: Lanza una instancia EC2 administrable por
#   AWS Systems Manager Session Manager, sin abrir SSH publico.
# Recursos creados:
#   - IAM Role con AmazonSSMManagedInstanceCore
#   - IAM Instance Profile
#   - Security Group sin reglas de ingreso
#   - EC2 Instance en subnet indicada
# Outputs:
#   - InstanceId
#   - InstanceProfileName
#   - RoleArn
# =============================================================
AWSTemplateFormatVersion: '2010-09-09'

Parameters:
  VpcId:
    Type: AWS::EC2::VPC::Id
    Description: VPC donde se creara el security group

  SubnetId:
    Type: AWS::EC2::Subnet::Id
    Description: Subnet donde se lanzara la instancia

  LatestAmiId:
    Type: AWS::SSM::Parameter::Value<AWS::EC2::Image::Id>
    Default: /aws/service/ami-amazon-linux-latest/al2023-ami-kernel-default-x86_64
    Description: AMI dinamica de Amazon Linux via SSM Parameter Store

  InstanceType:
    Type: String
    Default: t2.micro
    AllowedValues:
      - t2.micro
      - t3.micro
      - t3.small
    Description: Tipo de instancia EC2

Resources:
  IamRoleSsmInstance:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub cfn-lab-iam-role-ssm-instance-${AWS::StackName}
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: ec2.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/AmazonSSMManagedInstanceCore

  IamInstanceProfileSsm:
    Type: AWS::IAM::InstanceProfile
    Properties:
      InstanceProfileName: !Sub cfn-lab-iam-instance-profile-ssm-${AWS::StackName}
      Roles:
        - !Ref IamRoleSsmInstance

  SecurityGroupNoSsh:
    Type: AWS::EC2::SecurityGroup
    Properties:
      VpcId: !Ref VpcId
      GroupDescription: Security Group sin ingreso, administracion solo por SSM
      Tags:
        - Key: Name
          Value: !Sub cfn-lab-sg-no-ssh-${AWS::StackName}
      SecurityGroupEgress:
        - IpProtocol: -1
          CidrIp: 0.0.0.0/0

  Ec2InstanceSsmManaged:
    Type: AWS::EC2::Instance
    Properties:
      SubnetId: !Ref SubnetId
      ImageId: !Ref LatestAmiId
      InstanceType: !Ref InstanceType
      IamInstanceProfile: !Ref IamInstanceProfileSsm
      SecurityGroupIds:
        - !Ref SecurityGroupNoSsh
      Tags:
        - Key: Name
          Value: !Sub cfn-lab-ec2-ssm-managed-${AWS::StackName}

Outputs:
  Ec2InstanceId:
    Description: ID de la instancia EC2 administrable por SSM
    Value: !Ref Ec2InstanceSsmManaged

  IamInstanceProfileName:
    Description: Nombre del instance profile asociado
    Value: !Ref IamInstanceProfileSsm

  IamRoleArn:
    Description: ARN del rol IAM usado por la instancia
    Value: !GetAtt IamRoleSsmInstance.Arn
